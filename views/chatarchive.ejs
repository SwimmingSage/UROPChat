<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="/javascripts/chatarchive.js"></script>
  </head>
  <body>
    <div class="admin">
        <h1><%= title %></h1>
        <p>Welcome to Admin Overview Page of the Live Chats</p>
        <div class="styledbutton adminbutton" id="chatarchivebutton">
            <a href="/admin"><button>View Live Chats</button></a>
        </div>

    <!-- Objects are stored as 
        [ { _id: 593ee1363a25e87dcd634f9b,
        id: '593ee1363a25e87dcd634f9b',
        creationTime: '1497293110803',
        active: false,
        Conversation: [ [Object], [Object], [Object], [Object] ],
        Users: [ 'b', 'a' ],
        __v: 4 },
          { _id: 593ee1533a25e87dcd634fa0,
            id: '593ee1533a25e87dcd634fa0',
            creationTime: '1497293139236',
            active: true,
            Conversation: [ [Object], [Object], [Object], [Object] ],
            Users: [ 'a', 'b' ],
            __v: 4 } ] -->

        <% for(i=0; i < chats.length; i++) { %>
            <div class="archivechatboxes">
                <div class="upperarchive" id='<%= chats[i].id %>'>
                    <h1>Chat between <%= chats[i].Users[0] %> and <%= chats[i].Users[1] %></h1>
                    <span>&#10133</span>
                </div>
                <div class="archivechat" id="chat<%= chats[i].id %>">
                    <ul class="archivemessages">
                    <% for(x=0; x < chats[i].Conversation.length; x++) { %>
                        <% if (chats[i].Conversation[x].sender === chats[i].Users[0]) { %>
                            <li class="otheruser"><strong><%= chats[i].Conversation[x].sender %>:&nbsp;</strong><%= chats[i].Conversation[x].message %></li>
                        <% } else if (chats[i].Conversation[x].sender === chats[i].Users[1]){ %>
                            <li><strong><%= chats[i].Conversation[x].sender %>:&nbsp;</strong><%= chats[i].Conversation[x].message %></li>
                        <% } %>
                    <% } %>
                    </ul>
                </div>
            </div>

        <% } %>

    </div>
  </body>
</html>


